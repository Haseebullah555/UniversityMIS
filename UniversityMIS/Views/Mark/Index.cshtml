@model IEnumerable<UniversityMIS.Models.Marks>;
<div class="card">
    <div class="card-header d-flex justify-content-between align-items-center">
        <h5 class="mb-0">Marks</h5>
        <a asp-action="Create" class="btn btn-success btn-sm">
            <i class="bi bi-plus-lg"></i> Add New
        </a>
    </div>
    <div class="card-body">
        <table class="table table-striped table-hover">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Student</th>
                    <th>Semester</th>
                    <th>Subject</th>
                    <th>Mark</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var Mark in Model)
                {
                    <tr>
                        <td>@Mark.Id</td>
                        <td>@Mark.Student.FirstName</td>
                        <td>@Mark.Semester.SemesterName</td>
                        <td>@Mark.Subject.SubjectName</td>
                        <td>@Mark.Mark</td>
                        <td>
                            <div class="btn-group">
                                <a asp-action="Edit" asp-route-id="@Mark.Id" class="btn btn-primary btn-sm">Edit</a>
                                <form asp-action="Delete" asp-route-id="@Mark.Id" method="post" class="d-inline"
                                    onsubmit="return confirm('Are you sure you want to delete this Mark?');">
                                    <button type="submit" class="btn btn-danger btn-sm">Delete</button>
                                    @Html.AntiForgeryToken()
                                </form>

                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
