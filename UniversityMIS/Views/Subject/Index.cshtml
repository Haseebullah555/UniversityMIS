@model IEnumerable<UniversityMIS.Models.Subject>;
<div class="card">
    <div class="card-header d-flex justify-content-between align-items-center">
        <h5 class="mb-0">Subjects</h5>
        <a asp-action="Create" class="btn btn-success btn-sm">
            <i class="bi bi-plus-lg"></i> Add New
        </a>
    </div>
    <div class="card-body">
        <table class="table table-striped table-hover">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Subject Name</th>
                    <th>Semester</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var Subject in Model)
                {
                    <tr>
                        <td>@Subject.Id</td>
                        <td>@Subject.SubjectName</td>
                        <td>@Subject.Semester.SemesterName</td>
                        <td>
                            <div class="btn-group">
                                <a asp-action="Edit" asp-route-id="@Subject.Id" class="btn btn-primary btn-sm">Edit</a>
                                <form asp-action="Delete" asp-route-id="@Subject.Id" method="post" class="d-inline"
                                    onsubmit="return confirm('Are you sure you want to delete this Subject?');">
                                    <button type="submit" class="btn btn-danger btn-sm">Delete</button>
                                    @Html.AntiForgeryToken()
                                </form>

                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
