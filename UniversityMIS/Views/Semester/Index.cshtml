@model IEnumerable<Semester>;
<div class="card">
    <div class="card-header d-flex justify-content-between align-items-center">
        <h5 class="mb-0">Semesters</h5>
        <a asp-action="Create" class="btn btn-success btn-sm">
            <i class="bi bi-plus-lg"></i> Add New
        </a>
    </div>
    <div class="card-body">
        <table class="table table-striped table-hover">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Semester Name</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var semester in Model)
                {
                    <tr>
                        <td>@semester.Id</td>
                        <td>@semester.SemesterName</td>
                        <td>
                            <div class="btn-group">
                                <a asp-action="Edit" asp-route-id="@semester.Id" class="btn btn-primary btn-sm">Edit</a>
                                <form asp-action="Delete" asp-route-id="@semester.Id" method="post" class="d-inline"
                                    onsubmit="return confirm('Are you sure you want to delete this semester?');">
                                    <button type="submit" class="btn btn-danger btn-sm">Delete</button>
                                    @Html.AntiForgeryToken()
                                </form>

                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
